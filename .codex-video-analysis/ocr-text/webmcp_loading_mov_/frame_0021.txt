B Projects 0:45-1:30 — Run the briefing

B Local v

13 Pull requests ‘Type: “Restart the payments connection pool” ;
© ees Select: payments # main ;
ernione The most important tool is get_orgnenory_briefing . == Gonimit.or pushy

Instead of answering a general question, it evaluates an intended action. Here, the agent says it plans to © Compare branch a
s+ Explore restart the payments connection pool.

OrgMemory returns a clear verdict: requires approval. aes +
Recon It identifies two related incidents, the decision that capped worker concurrency, and the blast radius: payments
‘OrgMemory WebMCP Challenge shares a Postgres cluster with the ledger. It also provides the remembered first-response procedure. leader -Hibiieat ded 280: sE0872
CEHS ORANGES ETA Every claim includes a memory ID that a person can open and verify against its original evidence. WB codex-clipboard-ce7dd775-4e0b-4c

This briefing path is deterministic. Running the same intent against the same organizational memory produces s

codex-clipboard-b569de4c-16a0-4¢
& View all

Write Linkedin post on memory the same verdict. A production control should not change because a model phrased its answer differently.

‘Transform OrgMemory into WebMCP

: 1:30-1:50 — Show a read-only intent
‘Assess orgmemory WebMCP submis:

The boundary is based on the intended action—not simply the topic.
A read-only investigation can proceed. A potentially disruptive production action requires human approval.

Fix YouTube media downloads
Open Cosmos link in Chrome

List qualified job titles and ATS a }0-2:15 — Approvals queue

Evaluate vertical Al agent idea OrgMemory does not approve its own recommendation.

Reads are permission-trimmed to the user's team scope. Reporting an outcome only appends to the ledger.
And adding a fact to shared company memory requires a proposal that a human reviews.

see i oieen odennh eetie Capability is never treated as authorization.

Write GPT-5.6-level model code

Replace collage item 1

:15-2:40 — bs
Build Welspun pipe traceability galt

Serving the briefing opens an entry in the outcome leer.

Trim and combine videos ais
After acting—from GitHub, PagerDuty, or another sy. Y a—the agent calls record_orgmemory outcome with

Eeplain vouet] what it did and whether it worked,
Start service a
Review cosmoweb improvements
+ © Approve forme 5.6Sol High v @ +)

@ sanket.bhangale @
