01 / REMENBER

Incidents, decisions, and
owners stay tied to
evidence.

Code, postmortems, threads, and docs
become one time-aware memory
graph. Every promoted fact cites a
source; anything uncertain stays a
searchable chunk.

OrgMemory brings together incidents, decisions, dependencies, owners,
and runbooks—with evidence—so people and Al agents can check what
matters before they act.

Why is payments failing again? What changed in checkout this week? Who owns the ingestion pipeline?

© WebMCP ready © 27 read-only “9 human-governs =

02 / BRIEF

Agents get briefed before
they act, not after.

‘An agent about to touch a service asks
What this company knows first, and
gets back the decisions that constrain
it, the incidents that started the same
way, and the blast radius — anywhere
con the web, through WebMCP.

03 / GoveRN

Agents investigate.
People authorize.

Reads move at machine speed.
Anything that would change company
memory enters a scoped approval
queue and waits for a person.
Capability is never authorization

o4 7 cagrouno

Every answer is scored by
what happened next.

Context served, action taken, outcome
observed. The record of which context
actually produced correct action is the
one asset a better model cannot copy.

